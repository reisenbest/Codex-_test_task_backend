Тестовое задание для кодекс.
инструкция по запуску если через докер:
1. набрать в терминале pycharm "docker-compose up -d"
2. http://localhost:8001/ перейти суда

Либо просто развернуть проект на джанго в своей IDE и своем окружении. файл с зависимостями приложил

P.S. заранее прошу прощения, начинающий пока что (но хочу набраться опыта!), не приходилось ничего еще разворачивать чтобы показывать. А если надо
было показать - показывал через ngrok. так что как-то так...

P.S.S Не уверен, что правильно понял задание, но сделал, как понял. Инструкции по использованию внутри кода (dwh_project/urls.py)